# Project Overview — @@PROJECT_TITLE@@

> **TL;DR** — @@WHAT_IT_IS@@

## What it is

[GROUND: expand the TL;DR: the app's purpose, its 3-6 real features (from reading the
code — pages/models/classes/commands), and who uses it. If the repo had a pre-existing
README with unique content, fold that content in here — never discard information.]

## How it's put together

[GROUND: the app's shape end-to-end: entry point(s), the main modules/layers and what
each owns, where data lives, and how a request/run flows through. For a real app with
non-trivial architecture, split this into a sibling `architecture.md` and link it from
the index + tldr.]

## Tech stack

| Layer | Technology | Key details |
| --- | --- | --- |
[GROUND: the same rows as CLAUDE.md's stack table — keep the two in sync]

## Related docs

| Doc | Why |
| --- | --- |
| [getting-started.md](../02-setup/getting-started.md) | Set the project up from zero |
| [project-layout.md](../05-reference/project-layout.md) | The annotated file tree |
