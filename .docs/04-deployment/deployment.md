# Deployment — @@PROJECT_TITLE@@

> **TL;DR** — [GROUND: the honest deployment story. Kit default for this family:
> "This project runs locally only — no CI/CD, no deployment target." If a real deploy
> exists, describe it here instead.]

## Current state

[GROUND: either the honest "runs locally only; a developer machine + setup.ps1 is the
whole story — see getting-started" paragraph, or the real pipeline: trigger, build,
target, env vars, verification, rollback.]

## If this ever deploys

[GROUND: keep or delete. If runs-locally-only: one short paragraph on what would need
deciding (target, secrets management, CI) so the doc stays honest rather than
aspirational.]

## Related docs

| Doc | Why |
| --- | --- |
| [getting-started.md](../02-setup/getting-started.md) | The local "deployment" that actually exists |
| [conventions.md](../05-reference/conventions.md) | Git rules that protect main |
